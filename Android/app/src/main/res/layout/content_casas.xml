<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    android:background="@color/colorFondoApp">

    <ListView
        android:id="@+id/lvCasas"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:dividerHeight="0dp"
        android:layout_margin="@dimen/margen_default"
        android:divider="@color/colorFondoApp">
    </ListView>
    <include layout="@layout/viewconfirmar"/>
    <include layout="@layout/viewguardarcasa"/>
    <include layout="@layout/viewload"/>

</RelativeLayout>